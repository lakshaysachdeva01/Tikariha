<%- include('header', {seoDetails}) %>

<style>
    .thankyou-page {
        min-height: 100vh;
        display: flex;
        margin-top: -125px;
        align-items: center;
        justify-content: center;
        background: #fff;
        padding: 80px 20px;
        position: relative;
        overflow: hidden;
    }
    
    .thankyou-container {
        max-width: 800px;
        width: 100%;
        text-align: center;
        z-index: 1;
        position: relative;
    }
    
    .thankyou-content {
    
        backdrop-filter: blur(10px);
        border-radius: 20px;
        padding: 60px 40px;
  
    }
    
    .thankyou-title {
        font-family: var(--heading-font);
        color: #000000;
        font-size: 72px;
        font-weight: 500;
        margin-bottom: 30px;
        line-height: 1.2;
    }
    
    @media (max-width: 768px) {
        .thankyou-title {
            font-size: 48px;
        }
    }
    
    .thankyou-message {
        font-family: var(--body-font);
        color: var(--text-color);
        font-size: 18px;
        line-height: 1.6;
        margin-bottom: 40px;
        color: rgba(0, 0, 0, 0.8);
    }
    
    .thankyou-button {
        display: inline-block;
        padding: 18px 40px;
        background-color: var(--primary-color);
        color: var(--white-color);
        font-family: var(--body-font);
        font-size: 16px;
        font-weight: 600;
        text-decoration: none;
        border-radius: 50px;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
    }
    
    .thankyou-button:hover {
        background-color: #000000;
        transform: translateY(-2px);
        
        color: var(--white-color);
    }
    
    .thankyou-icon {
        font-size: 70px;
        color: var(--primary-color);
        margin-bottom: 30px;
        display: block;
    }
</style>

<div class="thankyou-page">
    <div class="thankyou-container">
        <div class="thankyou-content">
            <i class="fa-solid fa-check-circle thankyou-icon"></i>
            <h1 class="thankyou-title">Thank You!</h1>
            <p id="thank-you-message" class="thankyou-message">Your message has been sent successfully. We'll get back to you soon.</p>
            <a href="/" class="thankyou-button"><i class="fa-solid fa-arrow-left" style="margin-right: 15px;"></i>Go to Home</a>
        </div>
    </div>
</div>

<script>
    document.addEventListener("DOMContentLoaded", function () {
        const message = sessionStorage.getItem("thankYouMessage");
        
        if (message) {
            // Display the message in an element with ID 'thank-you-message'
            document.getElementById("thank-you-message").textContent = message;
    
            // Optionally clear the message after it's shown (to avoid persisting after page reload)
            sessionStorage.removeItem("thankYouMessage");
        }
    });
</script>

<%- include('footer') %>